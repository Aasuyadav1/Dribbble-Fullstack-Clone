/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'cdn.dribbble.com', 'https://plus.unsplash.com', 'lh3.googleusercontent.com', 'avatars.githubusercontent.com', 'plus.unsplash.com', 'res.cloudinary.com'],
    },
};

export default nextConfig;
